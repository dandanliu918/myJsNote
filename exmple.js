/** * Created by liudandan on 20162/21. */var exEmple = (function () {    return {        //1)累加        sum: function sum(num1, num2) {            var total = 0;            if (typeof num1 !== 'undefined') {                if (!isNaN(num1)) {                    total += Number(num1)                }            }            if (typeof  num2 !== 'undefined') {                if (!isNaN(num2)) {                    total += Number(num2);                }            }        },        //2)没有参数的累加        sum2: function sum2() {            var total = null;            for (var i = 0; i < arguments.length; i++) {                if (!isNaN(arguments[i])) {                    total += Number(arguments[i]);                }            }            return total;        },        //3)多个参数有乘 和 累加        sum3: function sum3(n1, n2, n3, n4) {            var total = null;            var chengji = 1;            for (var i = 0; i < arguments.length; i++) {                if (!isNaN(arguments[1])) {                    total += Number(arguments[i]);                    chengji *= Number(arguments[i]);                }            }            return {t: total, ji: chengji}            return total;            return chengji;        },        //4)升序        sort: function sort(a, b) {            return a - b        },        //5)获取ele所有的节点        children:function children(ele) {        var nodeChild = ele.childNodes;//获取所有的子节点        var ary = [];        for (var i = 0; i < nodeChild.length; i++) {            var cur = nodeChild[i];            if (cur.nodeType == 1) {                ary.push(cur);            }        }         return ary;      },        //6）获取 上一个元素哥哥节点        prevEleSibling:function prevEleSibling(ele) {        var prev = ele.previousSibling;//获取ele的哥哥节点        while (prev && prev.nodeType !== 1) {            prev = prev.previousSibling;        }        return prev;    },        //7）数组去重        aryqu:function aryqu() {        var ary = [1, 2, 3, 4, 2, 3, 5, 6, 7, 8, 8];        for (var i = 0; i < ary.length; i++) {        }        for (var j = i + 1; j < ary.length; j++) {            if (ary[i] === ary[j]) {                ary[j] = ary[ary.length - 1];//最后一项赋值给当前                j--;                ary.length--;            }        }    },       //8) 数组去重 （对象法）        quObjetary:function quObjetary() {        var ary = [1, 2, 3, 4, 3, 4];        var obj = {};        for (var i = 0; i < ary.length; i++) {            var cur = ary[i];            if (obj[cur] == cur) {                ary.splice(i, 1);                i--;                continue;            }            obj[cur] = cur        }    },       //9)验证码        Verification:function Verification() {           var box=document.getElementById('box');            function fn(){               var strRange='0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ';                Math.round(Math.random()*61);//获取随机的                var codeHtml="";//定义一个空字符                for(var i=0;i<4;i++){//循环四个                    var ran=Math.round(Math.random()*61);//获取随机的                    var ranChar=strRange.charAt(ran);//通过charAt 获取随机字母                    if(codeHtml.toUpperCase().indexOf(ranChar.toUpperCase())===-1){//获取并且 为-1 说明是符合条件                        codeHtml+=ranChar;                    }else {                        i--;                    }                }                box.innerHTML=codeHtml;            }            fn();//这里是刷新使用            box.onclick=fn;        },        //10)倒计时        Countdown:function () {           var timer=window.setInterval(record,1000);            function record() {                            }        },}})();/* 此文档不具备使用功能使用* exEmple.sum()* exEmple.sort()** **/